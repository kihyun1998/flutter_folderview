# thegraph build — flutter_folderview

## What this project is
A Flutter widget library rendering a semantic Folder → Parent → Child hierarchy;
identity and invariants are in `CLAUDE.md`.

## References
| Source | Informs | Reached by | Binding |
|---|---|---|---|
| just_tooltip | how it works | pub cache `just_tooltip-<ver>/` at the resolved version, or `../just_tooltip`; published version from `pub.dev/api/packages/just_tooltip` — raw | binding |
| flutter_table_plus | how it works | `../flutter_table_plus` — raw | example |
| Flutter framework | how it works | the SDK's `packages/flutter` source — raw | binding |
