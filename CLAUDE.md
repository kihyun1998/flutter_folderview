# CLAUDE.md

Project-level guidance for Claude Code when working in this repo.

## Agent skills

### Issue tracker

Issues live in GitHub Issues for `kihyun1998/flutter_folderview`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical label strings (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`) — no overrides. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
